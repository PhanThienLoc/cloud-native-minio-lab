# Member 3 – Week 1 Environment Setup

## 1. Docker

Lệnh:

```powershell
docker --version
```

Kết quả:

```text
Docker version 29.3.1, build c2be9cc
```

---

## 2. Git

Lệnh:

```powershell
git --version
```

Kết quả:

```text
git version 2.52.0.windows.1
```

---

## 3. MinIO Client (mc)

Lệnh:

```powershell
mc --version
```

Kết quả:

```text
mc version RELEASE.2025-08-13T08-35-41Z
Runtime: go1.24.6 windows/amd64
```

---

## 4. Kết luận

- [x] Docker đã được cài đặt.
- [x] Git đã được cài đặt.
- [x] MinIO Client (`mc`) đã được cài đặt và hoạt động bình thường.
- [x] Môi trường phát triển đã sẵn sàng cho các nhiệm vụ tiếp theo.