apiVersion: 1

providers:
  - name: MinIO Lab
    orgId: 1
    folder: MinIO Lab
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
